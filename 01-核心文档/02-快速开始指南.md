# 快速开始指南

本指南帮助你快速了解和使用LLM多模型评估项目。

---

## 🚀 5分钟快速体验

### 1. 查看评估结果

```bash
# 查看综合评估记录
cat LLM模型综合评估记录表.md

# 查看详细报告
cat LLM综合对比报告.md
```

**核心发现**:
- 🏆 **Codex-sol-2**: 2.96秒，首次LLM超越人类基准（3.16秒）
- ⚡ **hybrid_optimal**: 0.35秒，首次突破1秒大关
- 💰 **GLM-flash**: ¥1.8完成，性价比最高

---

## 📦 生成测试数据

### 快速测试（~1秒）

```bash
python generate_test_data.py --preset quick
```

生成：2,500样本，3.1M对，适合快速验证

### 中等测试（~10秒）

```bash
python generate_test_data.py --preset medium
```

生成：20,000样本，200M对，适合开发测试

### 大规模测试（~1分钟）

```bash
python generate_test_data.py --preset large
```

生成：75,000样本，2.8B对，接近真实场景

### 真实规模（~10分钟）

```bash
python generate_test_data.py --preset realistic
```

生成：200,000样本，20B对，完全真实规模

### 自定义配置

```bash
python generate_test_data.py \
    --num_identities 5000 \
    --samples_per_identity 20 \
    --noise_std 0.15 \
    --seed 42 \
    --output my_test.pkl
```

---

## ✅ 验证测试数据

```bash
# 验证数据质量
python validate_test_data.py test_data_medium.pkl

# 大数据集跳过采样
python validate_test_data.py test_data_realistic.pkl --no-sample
```

验证内容：
- ✓ 数据格式完整性
- ✓ L2归一化质量
- ✓ 样本对统计
- ✓ 相似度分布
- ✓ 可分性评估

---

## 💻 使用测试数据

### Python代码示例

```python
import pickle
import numpy as np

# 加载数据
with open('test_data_medium.pkl', 'rb') as f:
    features, _, labels, paths = pickle.load(f)

print(f"样本数: {len(labels):,}")
print(f"特征维度: {features.shape[1]}")
print(f"身份数: {len(np.unique(labels)):,}")

# 计算相似度
sim = features @ features.T
print(f"相似度矩阵形状: {sim.shape}")
```

### 完整示例

```bash
# 运行完整使用示例
python example_usage.py
```

这个示例展示：
1. 自动生成测试数据（如不存在）
2. 加载数据
3. 计算相似度直方图
4. 评估TPIR@FPIR指标

---

## 📂 查看各模型实现

### 性能最优实现

```bash
# Codex-sol-2 - 超越人类基准
cd codex-sol-2
cat face_similarity_evaluator.py

# Grok - 线程池最快
cd grok
cat eval_similarity.py
```

### 验证最完整实现

```bash
# DeepSeek - CPU float64验证
cd deepseek
cat step2_single_gpu.py
```

### 完整迭代过程

```bash
# Claude Opus - v1到v9演进
cd claude-opus
ls eval_v*.py
cat README.md
```

---

## 📊 预设配置对比

| 预设 | 样本数 | 样本对数 | 耗时 | 文件大小 | 适用场景 |
|------|-------|---------|------|---------|---------|
| quick | 2,500 | 3.1M | ~1秒 | ~5MB | 快速验证 |
| medium | 20,000 | 200M | ~10秒 | ~40MB | 开发测试 |
| large | 75,000 | 2.8B | ~1分钟 | ~150MB | 性能测试 |
| xlarge | 200,000 | 20B | ~10分钟 | ~400MB | 真实场景 |
| realistic | 200,000 | 20B | ~10分钟 | ~400MB | 完整评估 |

---

## 🎯 噪声参数调节

控制测试难度（noise_std参数）：

```bash
# 简单（理想情况）
python generate_test_data.py --preset medium --noise_std 0.10

# 推荐（接近真实）
python generate_test_data.py --preset medium --noise_std 0.15

# 中等难度
python generate_test_data.py --preset medium --noise_std 0.20

# 困难场景
python generate_test_data.py --preset medium --noise_std 0.30
```

**期望指标**（TPIR @ FPIR=1e-5）:
- 0.10: ~85% （理想）
- 0.15: ~62% （推荐）
- 0.20: ~45% （中等）
- 0.30: ~25% （困难）

---

## 📈 性能基准参考

### 统一基准测试结果

| 方法 | 总耗时 | 吞吐率 | 技术特点 |
|------|--------|--------|---------|
| hybrid_optimal | 0.35秒 | 59.41亿/秒 | ThreadPool+TF32 🏆 |
| grok_threadpool | 1.02秒 | 20.27亿/秒 | ThreadPool+FP32 🥈 |
| codex_tf32 | 27.47秒 | 0.75亿/秒 | Spawn+TF32 |
| deepseek_precise | 27.80秒 | 0.74亿/秒 | Spawn+FP32 |
| glm_highres | 28.34秒 | 0.73亿/秒 | Spawn+2M bins |

### LLM开发排名

| 模型 | 性能 | 开发时间 | 成本 | 评分 |
|------|------|---------|------|------|
| Codex-sol-2 | 2.96秒 | 77.3分钟 | ~$2 | 98分 🏆 |
| DeepSeek | 6.10秒 | 26.4分钟 | ~$1 | 90分 🥈 |
| Gemini场景 | 20.47秒 | 8.7分钟 | ~$1 | 92分 🥉 |
| Claude Opus | 27.32秒 | 231.3分钟 | ~$5 | 89分 |

---

## 💡 常见问题

### Q: 生成的数据和真实数据有什么区别？

A: 生成数据模拟真实特征分布，适合：
- ✅ 算法验证和开发
- ✅ 性能测试和优化
- ✅ 可复现的基准测试
- ❌ 不适合最终模型评估（需真实数据）

### Q: 如何选择合适的预设？

A: 根据目的选择：
- **quick**: 快速验证代码正确性
- **medium**: 日常开发测试
- **large**: 性能优化和调试
- **realistic**: 最终性能评估

### Q: 为什么Spawn方法这么慢？

A: Spawn进程启动开销~27秒，占用97%时间。解决方案：
- 使用ThreadPoolExecutor（hybrid/grok方法）
- 或使用fork模式（需Linux）

### Q: 如何获得最佳性能？

A: 三个关键：
1. 使用ThreadPoolExecutor（而非Spawn）
2. 启用TF32精度（PyTorch默认）
3. 使用对数切分负载均衡

---

## 🔗 相关文件

- `README.md` - 完整项目说明
- `LLM模型综合评估记录表.md` - 核心评估数据
- `generate_test_data.py` - 测试数据生成
- `validate_test_data.py` - 数据验证
- `example_usage.py` - 使用示例

---

## 📞 更多信息

查看完整文档：
```bash
cat README.md
```

查看评估记录：
```bash
cat LLM模型综合评估记录表.md
```

查看帮助信息：
```bash
python generate_test_data.py --help
python validate_test_data.py --help
```

---

**开始使用**:
```bash
# 1. 生成测试数据
python generate_test_data.py --preset medium

# 2. 验证数据
python validate_test_data.py test_data_medium.pkl

# 3. 运行示例
python example_usage.py
```

祝你使用愉快！🚀
