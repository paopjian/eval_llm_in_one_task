"""统一评估框架：共享读取/校验/指标，各模型仅提供核心计算 core。

目录结构:
    common.py      共享实现：数据读取、直方图网格、校验、TPIR@FPIR 指标
    cores/         各模型 core（核心计算），继承自各模型目录中的原实现
    run_one.py     单模型执行器（子进程）：加载数据 -> core -> 校验 -> 指标
    run_eval.py    评估调度器：超时控制、内存上限、200K/2M 两阶段流程
"""
