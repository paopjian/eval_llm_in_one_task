# 任务运行统计记录

> 任务：人脸特征相似度矩阵计算与评估（读取 `任务文档.md` → 编写代码 → 全量计算 → TPIR@FPIR → 可视化/样本对提取）
> 统计来源：DSH 会话日志 `session.jsonl.zstd`（时间戳精确到毫秒）＋ 程序自身计时（见《评估报告.md》）
> 数据截至：2026-09-05 17:58:14（本记录生成时刻；模型 `deepseek-v4-flash`，provider `deepseek-official`）

---

## 1. 时间统计

### 1.1 会话整体（含当前回合）

| 项目 | 数值 |
|---|---|
| 会话开始 | 2026-09-05 17:23:59 |
| 统计截至 | 2026-09-05 17:58:14 |
| 会话总时长 | **约 34.2 分钟**（2054 秒，含等待/思考/问答） |

### 1.2 任务主体（代码编写与执行，第 2 回合）

| 项目 | 数值 |
|---|---|
| 开始（用户下达任务） | 17:26:25 |
| 完成（提交评估报告与结果） | 17:52:51 |
| 任务用时 | **约 26.4 分钟**（1586 秒） |

其中模型+工具实际忙碌时间（63 个 step 累计，含 GPU 程序运行等待）：**约 26.1 分钟**。中途主要耗时点：一次子进程 fork 死锁（约 10 分钟无响应后被超时终止）→ 改为子进程架构重写后问题解决；其后各步均秒级~分钟级完成。

### 1.3 评估程序本身运行耗时（GPU 程序计时，与上表是两个维度）

| 阶段 | 耗时 |
|---|---|
| 数据载入（20.3 万样本 pkl） | ~0.3 s |
| 全量相似度统计（206.5 亿对，约 21.1 万亿次浮点） | 7 卡墙钟 6.1 s（GPU 纯算每卡 0.5~0.7 s）；单卡 5.7 s |
| TPIR@FPIR 指标计算 | <0.1 s |
| 出图（分布图+曲线图） | ~1.4 s |
| 一键全流程（含样本对提取二次遍历 27~30 s） | 41.5 s |

## 2. Token 统计（来自会话日志 usage 字段）

### 2.1 汇总（模型侧全部请求）

| 口径 | Tokens |
|---|---|
| 输入（新，未命中缓存） | 54,742 |
| 缓存读取（cacheRead，重复上下文） | 5,310,848 |
| 输出合计 | 108,154 |
| 其中推理（reasoning）token | 62,203（占输出 57.5%） |
| 计费口径合计（输入+缓存+输出） | **5,473,744** |

> 注：缓存读取占比 97.0% —— 主要来自任务主回合 53 次模型请求中对同一长上下文的重复读取；实际"新生成"内容约 10.8 万 token。

### 2.2 分回合明细

| 回合 | 内容 | 请求次数 | 输入(新) | 缓存读取 | 输出 | 推理 |
|---|---|---|---|---|---|---|
| 1 | 打招呼 | 1 | 8,087 | 0 | 202 | 32 |
| 2 | 任务主体（读文档→编码→执行→验证） | 53 | 32,791 | 4,066,048 | 94,661 | 53,991 |
| 3 | 详细结果写入 md + 耗时 | 3 | 6,230 | 350,464 | 5,775 | 3,005 |
| 4 | 单卡 vs 七卡启动开销答疑 | 2 | 3,327 | 246,016 | 4,417 | 3,300 |
| 5 | 是否全量计算答疑 | 1 | 1,437 | 126,208 | 1,030 | 463 |
| 6 | 本回合（统计记录，进行中） | ≥4 | 2,870 | 522,112 | 2,069 | 1,412 |
| 合计 | — | 64 | 54,742 | 5,310,848 | 108,154 | 62,203 |

### 2.3 工具调用统计（共 61 次）

| 工具 | 次数 | 用途 |
|---|---|---|
| bash | 29 | 环境检查、运行 step1~5/run_eval、性能测试、日志统计 |
| write | 14 | 编写核心库/step 脚本/报告 |
| edit | 10 | 修复 bug、参数调整 |
| todo_write | 4 | 任务阶段管理 |
| read | 3 | 任务文档/已有代码/代码片段 |
| read_image | 1 | 图片校验（模型不支持读图，失败） |

## 3. 费用估算

系统未记录实际账单（本地 Harness 无计价字段、模型为内部 `deepseek-v4-flash`），以下为**按公开 API 价格的估算**：

| 价格档位（$/1M tokens） | 输入新 | 缓存命中 | 输出 | 估算费用 |
|---|---|---|---|---|
| A. 近似 deepseek-chat 档（0.27 / 0.07 / 1.10） | 54,742×0.27 | 5,310,848×0.07 | 108,154×1.10 | **≈ 0.51 USD（约 ¥3.7）** |
| B. 近似 deepseek-reasoner 档（0.55 / 0.14 / 2.19） | 54,742×0.55 | 5,310,848×0.14 | 108,154×2.19 | **≈ 1.01 USD（约 ¥7.3）** |

- 说明：费用几乎全部来自缓存读取（5.31M token，占计费量 97%）与 10.8 万输出 token；
- 若部署侧为内部/不计费通道，实际费用为 **0 元**；
- 汇率按 1 USD ≈ 7.2 CNY 折算。

## 4. 一句话总结

**任务主体用时约 26 分钟（会话累计 ~34 分钟），评估程序本身 7 卡跑全库 206.5 亿对仅需 6.1 秒、出图 ~12 秒；模型共消耗约 547 万 token（97% 为缓存读取、输出 10.8 万含推理 6.2 万），估算费用约 0.5~1.0 美元（内部不计费则为 0）。**
