# LLM自迭代任务：大规模人脸特征相似度矩阵计算与评估

## 任务背景

本任务旨在测试LLM的自我迭代能力，从最基础的特征读取和单卡NxN相似度计算开始，逐步演进到能够高效处理大规模数据的多卡并行评估系统，最终实现TPIR@FPIR指标计算和可视化。

## 环境说明

- **Python环境**: `/root/miniconda3/envs/cvlface`（已激活）
- **可用显卡**: 7张GPU（cuda:0 到 cuda:6）
- **库限制**: 禁止安装任何新库，只能使用环境中已有的库
- **数据路径**: `s4_0618_enhance.pkl`（当前目录）
- **中文字体**: `font/SourceHanSansSC-Normal.otf`（当前目录）

## 数据格式说明

特征文件是用模型提取后保存的pkl文件，包含以下数据：

```python
import pickle

# 读取数据
with open('s4_0618_enhance.pkl', 'rb') as f:
    query_feats_list, query_feats_list_flip, query_ids, file_paths = pickle.load(f)

# 数据说明
# query_feats_list: numpy array, shape=(N, 512)
#                   L2归一化的特征向量，每行是一张图片的512维特征
# 
# query_feats_list_flip: list (空列表，本任务不使用)
# 
# query_ids: numpy array, shape=(N,)
#           身份标签，相同标签表示同一个人
#           例如: [0, 0, 0, 1, 1, 2, 2, 2, 3, ...]
# 
# file_paths: list of str
#            图片路径列表，与特征一一对应
```

## 任务目标

**最终目标**：启动代码后以最快速度获取TPIR@FPIR曲线和相似度分布图。

**核心功能要求**：
1. 读取特征文件并理解数据结构
2. 计算NxN相似度矩阵，区分正负样本
3. 计算TPIR@FPIR指标（见下方说明）
4. 支持提取above/below阈值的样本对（用于错误分析）
5. 绘制评估图表（相似度分布图、TPIR@FPIR曲线）
6. 支持多卡并行加速

**性能要求**：
- 能够处理大规模数据（样本数可能达到数万，样本对数达到亿级）
- 计算速度尽可能快
- 内存占用合理

## 功能演进建议

### 第一步：基础实现

**1.1 特征读取**
- 读取pkl文件
- 理解数据格式：特征矩阵、身份标签
- 统计正负样本对数量（同一身份为正样本对，不同身份为负样本对）

**1.2 单卡相似度计算**
- 在单张GPU上计算特征矩阵的余弦相似度（特征已L2归一化，可直接矩阵乘法）
- 只计算上三角矩阵（避免重复计算）
- 根据身份标签区分正样本和负样本
- 收集正负样本的相似度值

**1.3 TPIR@FPIR计算**
- 基于收集的相似度值计算TPIR@FPIR曲线
- 输出关键评估点

### 第二步：优化和扩展

**2.1 内存优化**
- 分块计算避免OOM
- 探索更高效的数据统计方法（如果遇到内存或存储瓶颈）

**2.2 多卡并行**
- 将计算任务分配给多个GPU
- 实现负载均衡
- 合并各GPU结果

**2.3 样本对提取**
- 支持提取相似度高于/低于指定阈值的样本对
- 用于后续错误分析

**2.4 可视化**
- 绘制正负样本相似度分布
- 绘制TPIR@FPIR曲线

### 第三步：持续优化

根据实际运行中遇到的瓶颈（内存、速度、存储等）自行探索优化方案。

## 性能优化提示

1. **计算优化**：
   - 特征已L2归一化，相似度 = 特征矩阵乘法
   - 只计算上三角，节省50%计算
   - 分块计算避免OOM

2. **多卡优化**：
   - 负载均衡的任务分配
   - 避免GPU闲置

3. **数据优化**：
   - 如果遇到内存或存储瓶颈，考虑是否需要保存所有中间数据
   - 思考如何用统计方法代替全量数据保存

4. **精度优化**：
   - 评估结果保证合理精度即可（相似度一般看到小数点后3-4位）
   - 过高精度可能带来不必要的计算和存储开销

## 评估指标说明

### 正负样本定义
- **正样本对**：`query_ids[i] == query_ids[j]` 且 `i < j`
- **负样本对**：`query_ids[i] != query_ids[j]` 且 `i < j`

### TPIR@FPIR计算
```
给定阈值 threshold:
FPIR(threshold) = (负样本中 sim > threshold 的数量) / (总负样本数)
TPIR(threshold) = (正样本中 sim > threshold 的数量) / (总正样本数)

常见评估点：
- TPIR @ FPIR=1e-3
- TPIR @ FPIR=1e-4  
- TPIR @ FPIR=1e-5
```

### 预期结果范围

本数据集的正确TPIR@FPIR结果应在以下范围内（供验证使用）：

```
TPIR @ FPIR=1e-5: 约 60-65%
TPIR @ FPIR=1e-4: 约 82-85%
TPIR @ FPIR=1e-3: 约 90-93%
TPIR @ FPIR=1e-2: 约 95-97%
```

**注意**：如果你的结果与上述范围偏差较大（>5%），请检查：
1. 正负样本是否正确区分
2. 相似度计算是否正确（特征已L2归一化，直接矩阵乘法即可）
3. TPIR/FPIR的定义是否正确实现

如果结果在合理范围内，请继续优化性能（计算速度、内存占用）。

## 测试启动代码

以下是一个简单的启动代码，帮助你理解数据结构：

```python
#!/usr/bin/env python3
"""
测试启动代码：读取数据并统计基本信息
"""
import pickle
import numpy as np

def load_and_analyze_data():
    """读取并分析测试数据"""
    print("=" * 80)
    print("数据加载与分析")
    print("=" * 80)
    
    # 1. 读取数据
    with open('s4_0618_enhance.pkl', 'rb') as f:
        query_feats_list, query_feats_list_flip, query_ids, file_paths = pickle.load(f)
    
    print(f"\n数据基本信息:")
    print(f"  特征矩阵形状: {query_feats_list.shape}")
    print(f"  特征维度: {query_feats_list.shape[1]}")
    print(f"  样本总数: {len(query_ids)}")
    print(f"  身份总数: {len(np.unique(query_ids))}")
    
    # 2. 统计正负样本对数量
    N = len(query_ids)
    total_pairs = N * (N - 1) // 2
    
    # 计算正样本对数量
    unique_ids, counts = np.unique(query_ids, return_counts=True)
    total_pos_pairs = sum(c * (c - 1) // 2 for c in counts)
    total_neg_pairs = total_pairs - total_pos_pairs
    
    print(f"\n样本对统计:")
    print(f"  总样本对数: {total_pairs:,}")
    print(f"  正样本对数: {total_pos_pairs:,} ({total_pos_pairs/total_pairs*100:.2f}%)")
    print(f"  负样本对数: {total_neg_pairs:,} ({total_neg_pairs/total_pairs*100:.2f}%)")
    
    # 3. 检查特征是否归一化
    norms = np.linalg.norm(query_feats_list, axis=1)
    print(f"\n特征向量检查:")
    print(f"  L2范数均值: {norms.mean():.6f}")
    print(f"  L2范数标准差: {norms.std():.6f}")
    print(f"  是否归一化: {'是' if np.abs(norms.mean() - 1.0) < 0.01 else '否'}")
    
    print("\n" + "=" * 80)
    print("数据分析完成！")
    print("=" * 80)
    
    return query_feats_list, query_ids, file_paths

if __name__ == '__main__':
    query_feats_list, query_ids, file_paths = load_and_analyze_data()
    
    # 下一步：实现相似度计算...
```

运行此代码：
```bash
/root/miniconda3/envs/cvlface/bin/python test_load_data.py
```

## 代码要求

1. **从零开始编写**：不依赖任何现有实现，展示完整演进过程
2. **环境兼容**：只使用cvlface环境中已有的库（torch, numpy, tqdm, matplotlib等）
3. **中文支持**：绘图使用指定中文字体
4. **参数化**：支持命令行参数传入阈值等配置
5. **清晰输出**：打印关键统计信息（样本对数量、计算耗时、评估结果）

## 成功标准

- ✅ 能够正确读取特征文件
- ✅ 正确区分正负样本对并统计数量
- ✅ TPIR@FPIR计算正确
- ✅ 能够提取above/below阈值的样本对
- ✅ 生成清晰的可视化图表
- ✅ 支持多卡并行加速
- ✅ 能够处理大规模数据（样本对数达到亿级）
- ✅ 总计算时间尽可能短

## 注意事项

1. **从零开始编写**：展示完整的迭代演进过程
2. **遇到瓶颈时主动优化**：如果发现内存、存储、速度问题，自行探索解决方案
3. **迭代式开发**：从简单到复杂，每个版本都能运行
4. **实用性优先**：最终目标是快速获得准确的评估结果

---

**开始任务提示词示例**：
```
请实现一个人脸特征相似度评估系统。读取 s4_0618_enhance.pkl 文件，
计算NxN相似度矩阵，区分正负样本，计算TPIR@FPIR指标，并绘制评估图表。
要求：使用cvlface环境，支持7卡并行，不安装新库，处理大规模数据时注意内存优化。
```
